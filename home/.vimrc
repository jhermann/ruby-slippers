" vim defaults

" 256 colors
set t_Co=256
set background=dark
set modeline
set modelines=5

" use those colors
syntax enable

" unify system and vim paste buffers (on newer vim / OS?)
set clipboard=unnamedplus
