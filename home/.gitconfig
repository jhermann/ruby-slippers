[core]
    autocrlf = input
    excludesfile = ~/.gitignore_global
[user]
    name = nobody
    email = nobody@example.com
[color]
    diff = auto
    status = auto
    branch = auto
[alias]
    st = status
    ci = commit
    df = diff
    dc = diff --cached
